Installation
============

DerivKit is currently distributed from source.

.. code-block:: bash

   git clone https://github.com/derivkit-org/derivkit.git
   cd derivkit
   pip install -e .

For development:

.. code-block:: bash

   pip install -e ".[dev]"
